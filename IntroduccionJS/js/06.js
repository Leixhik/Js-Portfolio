// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); // Se queda en la posición
resultado = Math.ceil(2.2); // Ceil siempre redondea hacia arriba
resultado = Math.floor(2.9); // Floor siempre redondea hacia abajo
resultado = Math.sqrt(144); // raíz cuadrada
resultado = Math.abs(-200); // Resultado siempre positivo
resultado = Math.min(3, 5, 1, 0, 2, 10) // Valor mínimo
resultado = Math.max(3, 5, 1, 0, 2, 10) // Valor mínimo
resultado = Math.random();
resultado = Math.floor(Math.random() * 30); // Integrar método en otro método


console.log(resultado);